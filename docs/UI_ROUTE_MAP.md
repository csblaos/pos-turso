# UI Route Map

ไฟล์นี้ map หน้า UI -> component หลัก -> API ที่เรียก เพื่อ trace ปัญหาได้เร็ว

## Notes

- หน้าแบบ server component บางหน้า query DB ตรงใน server action/query layer โดยไม่ยิง `/api/*`
- ตารางนี้เน้น flow หลักที่มีการเรียก API จากฝั่ง UI โดยตรง
- global header (`components/app/app-top-nav.tsx`) มี quick notification inbox (bell) เรียก `GET/PATCH /api/settings/notifications/inbox` และมีลิงก์ไป `/settings/notifications`; desktop ใช้ anchored popover, ส่วนจอ non-desktop (`<1200px`) ใช้ fixed-centered popover + จำกัดความสูง `~68dvh`
- global header (`components/app/app-top-nav.tsx`) มี quick store switcher จากปุ่ม `เปลี่ยนร้าน`: เปิด `SlideUpSheet` บนหน้าเดิมและ reuse `components/app/stores-management.tsx` (`mode="quick"`) โดยเรียก `POST /api/stores/switch` และ `POST /api/stores/branches/switch` โดยตรง แทนการพาไป `/settings/stores`
- หน้า `/stock?tab=purchase` ใช้ workspace switcher ในหน้าเดียว (`PO Operations` / `Month-End Close` / `AP by Supplier`) เป็นบล็อกนำทางแยกจาก KPI โดย reuse API เดิมทั้งหมด และจำโหมดล่าสุดผ่าน query `workspace` + localStorage; KPI strip (`Open PO`, `Pending Rate`, `Overdue AP`, `Outstanding`) เป็น summary-only (ไม่คลิก), ส่วน shortcut ใช้ saved preset ต่อผู้ใช้ (localStorage) และ sync filter ลง URL (`poStatus`, `due`, `payment`, `sort`) โดย `PO Operations` ใช้ default filter เป็น `OPEN`
- หน้า `/orders/new` ถ้าสินค้ามีหลายหน่วยขาย (`1 / 100 / 1000`) การกด quick add หรือสแกนจะเปิด `SlideUpSheet` ให้เลือกหน่วยขายก่อนเพิ่มเข้าตะกร้า และ quantity cap ใน cart จะคำนวณตาม `multiplierToBase` ของหน่วยที่เลือก

## Dashboard

| Page | Main Component | API Calls |
|---|---|---|
| `/dashboard` | `components/storefront/dashboard/registry.tsx` | ไม่มี browser call ตรงไป `/api`; ใช้ server query `getDashboardViewData` เพื่อโหลด metrics + low stock + AP reminders (`overdue`/`due soon`); UI รอบล่าสุดเป็น mobile-first work dashboard โดย map metric เดิมเป็นบล็อก `งานวันนี้` (deep-link ไป `/orders`, `/stock?tab=inventory`, `/stock?tab=purchase`), ตัด shortcut cards ที่ซ้ำกับ navigator ออก, และคงไว้แค่ลิงก์ `รายงาน` จุดเดียว |

## Auth & Onboarding

| Page | Main Component | API Calls |
|---|---|---|
| `/login` | `components/app/login-form.tsx` | `POST /api/auth/login` (spinner + disable state ระหว่าง submit, success ใช้ client-side transition แทน full reload; ไม่มี CTA `Sign up` แล้ว และแสดง helper text ให้ติดต่อ `Superadmin / System Admin` แทนเพราะบัญชีถูก provision โดยผู้ดูแลระบบ) |
| `/signup` | `components/app/signup-form.tsx` | `POST /api/auth/signup` |
| `/onboarding` | `components/app/onboarding-wizard.tsx` | `GET/POST /api/onboarding/channels`, `POST /api/onboarding/store`, `POST /api/auth/logout` |

## Orders

| Page | Main Component | API Calls |
|---|---|---|
| `/orders` | `components/app/orders-management.tsx` | `GET /api/orders`, `GET /api/orders/[orderId]`, `PATCH /api/orders/[orderId]`, `POST /api/orders/cod-reconcile` (หน้า manage ออเดอร์ที่สร้างแล้ว; CTA หลักเป็นปุ่ม `เข้าโหมด POS` เพื่อไป `/orders/new` และถอด quick create (`สร้างด่วน`) ออกจากหน้า manage; ถ้า URL ยังไม่มี `tab`, server จะเลือก default queue จาก role/permissions ก่อน render; เคส `READY_FOR_PICKUP` แสดงสถานะย่อยเพิ่มจาก `paymentStatus` เป็น badge รอง `ค้างจ่าย`/`ชำระแล้ว`/`รอตรวจสลิป` ทั้ง mobile+desktop; online order ปรับ badge สถานะใหม่ให้แยกงานกับการชำระมากขึ้น โดย `PENDING_PAYMENT` แสดง badge หลัก `รอดำเนินการ` และมี badge รองจาก `paymentMethod/paymentStatus` เช่น `ยังไม่ชำระ`, `รอตรวจสลิป`, `COD`, `COD รอปิดยอด`, `ชำระแล้ว`; desktop table เพิ่มคอลัมน์ `ช่องทาง` แสดงรูปแบบ `Facebook • LAK • COD` และให้คอลัมน์ `ยอดรวม` เหลือเฉพาะยอดเงิน; หน้า list เริ่มเป็น work queue มากขึ้นโดยเพิ่ม `งานถัดไป` และ `การทำงาน` สำหรับ quick action ที่ทำได้จากหน้า list เลย (`confirm_paid`, `mark_packed`, `mark_shipped`, `submit_for_payment`) และเคสที่ต้องกรอกข้อมูลเพิ่มอย่าง `Walk-in/Pickup + ON_CREDIT` หรือ `COD รอปิดยอด` จะเปิด review sheet จากหน้า list แทนการบังคับเข้า detail เต็มหน้า; สำหรับ `COD รอปิดยอด` ถ้าผู้ใช้มีสิทธิ์ `orders.cod_return` จะมี action รอง `ตีกลับ` จากหน้า list เพิ่มด้วย และใช้ review sheet กรอก `ค่าตีกลับเพิ่ม + เหตุผล/หมายเหตุ` ก่อนยิง `PATCH /api/orders/[orderId]` action `mark_cod_returned`; รอบนี้เพิ่ม multi-select + sticky bulk action bar และยิง `PATCH /api/orders/[orderId]` ทีละรายการสำหรับ bulk routine actions เดิม โดยคงรายการที่ fail ไว้ให้ตรวจต่อ; เพิ่ม selection summary และ bulk print `ใบเสร็จ`/`ป้ายจัดส่ง`/`ใบแพ็ก` จาก selection โดยดึง `GET /api/orders/[orderId]` ของแต่ละรายการมา merge เป็น current-page print document ชุดเดียว; bulk pack จะดึง detail ของแต่ละออเดอร์แล้วกรองเฉพาะรายการที่เปิด pack view ได้; เพิ่ม pack view จากหน้า list โดยปุ่ม `หน้าแพ็ก` จะดึง `GET /api/orders/[orderId]` ตอนเปิด sheet เท่านั้น แล้วพิมพ์ `ใบแพ็ก` ผ่าน current-page print ได้จากหน้า `/orders` โดยไม่ต้องเข้า detail; เพิ่ม work-queue tabs แบบ server-backed ผ่าน query `tab` (`ALL`, `PAYMENT_REVIEW`, `TO_PACK`, `TO_SHIP`, `PICKUP_READY`, `COD_RECONCILE`) พร้อม count badge ต่อคิวงานจาก server; รอบนี้เพิ่ม query `q` สำหรับ search แบบ server-backed และปุ่มสแกนบนหน้า list โดย scanner จะ parse `QR ออเดอร์` รูปแบบ `ORDER:${orderNo}` มา apply เป็น search ให้เลย; review sheet `ยืนยันรับชำระ` จะกรองบัญชี QR ตาม `paymentCurrency` ของออเดอร์แล้ว; บรรทัด meta ของ mobile ใช้ข้อความสั้นแบบเดียวกัน `Facebook`/`WhatsApp`/`Walk-in`/`Pickup`) |
| `/orders/new` | `components/app/orders-management.tsx` (`mode=create-only`) | `POST /api/orders`, `GET /api/orders/[orderId]`, `GET /api/orders`, `PATCH /api/orders/[orderId]`, `GET /api/orders/payment-accounts/[accountId]/qr-image` (POS-style create flow: หน้าเลือกสินค้าแบบ card grid + toolbar `ค้นหา/สแกน` + category chips ใต้ search + sticky cart bar; product card แสดงรูปย่อสินค้า; ซ่อน bottom tab navigation ระหว่างอยู่หน้า create เพื่อโฟกัสงานและเพิ่มพื้นที่บนมือถือ; ใช้ปุ่ม back บน navbar เป็น `กลับรายการออเดอร์` พร้อม confirm เมื่อมี draft ค้าง; ขั้น checkout ใช้ CTA หลักเดียวสำหรับบันทึกและให้กลับไปแก้สินค้าได้จากปุ่มด้านบนใน sheet; checkout แยกประเภทออเดอร์เป็น `Walk-in ทันที` / `มารับที่ร้านภายหลัง` / `สั่งออนไลน์/จัดส่ง` และแสดง field+validation ตาม flow (ที่อยู่+COD เฉพาะออนไลน์, `Walk-in/Pickup` ไม่บังคับชื่อ/เบอร์); โหมดออนไลน์เพิ่ม section `ข้อมูลขนส่ง` แบบเลือกผู้ให้บริการเป็นปุ่ม grid จาก `catalog.shippingProviders` + `อื่นๆ` โดยเริ่มต้นค่า provider ว่างและต้องเลือกเองก่อนสร้างออเดอร์ (ถ้า `อื่นๆ` กรอกชื่อเพิ่มได้), ส่ง `shippingCarrier` เป็นค่าว่างจาก UI; ช่อง `ค่าขนส่ง` (`ค่าส่งที่เรียกเก็บ`/`ต้นทุนค่าส่ง`) เป็น panel พับ/เปิด default ปิดและปิดแล้วรีเซ็ตเป็น `0`; บน desktop จัดการ์ด `ส่วนลด` และ `ค่าขนส่ง` เป็น 2 คอลัมน์เท่ากัน; วิธีรับชำระเป็นปุ่ม chips ตาม flow (`เงินสด/QR/ค้างจ่าย` และเพิ่ม `COD` เฉพาะออนไลน์); เมื่อเลือก `QR` ระบบจะกรองบัญชีตาม `paymentCurrency` ของออเดอร์และแสดงเฉพาะบัญชีที่มี QR พร้อม `account.currency` ตรงกับสกุลนั้น; เมื่อเลือกบัญชี `QR` จะมี section `แสดง QR` แบบพับ/เปิด (default ปิด) เพื่อดูรูป QR, ข้อมูลบัญชี, คัดลอกเลขบัญชี, เปิดรูปเต็มใน overlay หน้าเดิม, และดาวน์โหลดผ่าน route same-origin เมื่อจำเป็น โดยมี action รอง `เปิดแท็บใหม่` ภายใน overlay; scanner ใช้โครงเดียวกับ `/orders` (`BarcodeScannerPanel` + permission sheet มาตรฐาน); เพิ่มปุ่ม `ล่าสุด` ใต้แถบค้นหาเพื่อเปิด `SlideUpSheet` รายการ 8 ออเดอร์ล่าสุดจาก `GET /api/orders` และกด `เปิดสรุป` เพื่อ reopen success action sheet ได้; ในรายการล่าสุดสามารถกด `ยกเลิก` ผ่าน modal กลางได้ 2 โหมด: ถ้าเป็น `Owner/Manager` ใช้ `เหตุผล + สไลด์ยืนยัน` (`approvalMode=SELF_SLIDE`), ถ้าเป็น role อื่นใช้ `ยืนยันรหัสผ่าน Manager` (`approvalMode=MANAGER_PASSWORD`) และมี cooldown เมื่อยืนยันไม่ผ่านติดกันหลายครั้ง; matrix สถานะเริ่มต้นล่าสุดคือ `Walk-in จ่ายแล้ว => PAID+ตัดสต็อก`, `Walk-in ค้างจ่าย => PENDING_PAYMENT+จอง`, `Pickup later => READY_FOR_PICKUP+จอง`, `Online delivery => PENDING_PAYMENT+จอง` (รองรับต่อใน detail ด้วยสถานะกลาง `PICKED_UP_PENDING_PAYMENT` เมื่อลูกค้ารับสินค้าไปก่อนแต่ยังไม่จ่าย); หลังสร้างสำเร็จทุกหน้าจอจะแสดง success action sheet พร้อม preview บิลและโหลดข้อมูลออเดอร์จริงผ่าน `GET /api/orders/[orderId]`; ปุ่มพิมพ์ใบเสร็จ/สติ๊กเกอร์ใช้ `window.print()` + print-root ในหน้าเดิม และปุ่มพิมพ์จะรอจน preview โหลดพร้อมก่อน (ไม่เปิดแท็บใหม่/ไม่เปลี่ยนหน้า); รองรับราคาหน่วยแปลงแบบกำหนดเองจากสินค้า โดย fallback เป็น `ราคาหน่วยหลัก x ตัวคูณ` หากไม่กำหนด และรอบล่าสุด `catalog.products` จะส่งเฉพาะหน่วยที่เปิดขาย (`allow_base_unit_sale` / `enabled_for_sale`) เพื่อซ่อนการขายเป็นชิ้นหรือหน่วยอ้างอิงที่ไม่ต้องการจาก POS; fallback ชื่อลูกค้าอัตโนมัติเมื่อเว้นว่าง) |
| `/orders/cod-reconcile` | `components/app/orders-cod-reconcile.tsx` | `GET /api/orders/cod-reconcile`, `POST /api/orders/cod-reconcile`, `PATCH /api/orders/[orderId]` (หน้า reconcile สำหรับปิดยอด COD รายวันแบบ batch: กรองช่วงวันที่/ขนส่ง/คำค้น, แก้ `ยอดโอนจริง` + `codFee` ต่อรายการ, มี summary real-time ของรายการที่เลือกและร่างข้อมูลในหน้าปัจจุบัน, เลือกหลายรายการแล้วปิดยอดทีเดียว; ใช้สิทธิ์ `orders.mark_paid`, โดยฝั่งปิดยอดส่ง `Idempotency-Key` กันคำขอซ้ำ; ถ้ามีสิทธิ์ `orders.cod_return` จะมี action รอง `ตีกลับ` ต่อแถว เปิด review sheet กรอก `ค่าตีกลับเพิ่ม + เหตุผล/หมายเหตุ` แล้วค่อยยิง `mark_cod_returned` ได้จากหน้านี้โดยตรง; UI รอบล่าสุดปรับเป็น mobile-first workspace, ใช้ shared custom date picker แทน `<input type="date">`, และมี sticky batch bar ด้านล่างเมื่อมี selection) |
| `/orders/[orderId]` | `components/app/order-detail-view.tsx` | `PATCH /api/orders/[orderId]`, `POST /api/orders/[orderId]/shipments/upload-label`, `GET /api/orders/payment-accounts/[accountId]/qr-image` (detail action รองรับ COD flow: แพ็กจาก `PENDING_PAYMENT` ได้, ปุ่มยืนยันชำระเปลี่ยนเป็น `ยืนยันรับเงินปลายทาง (COD)` เมื่อ `SHIPPED + COD_PENDING_SETTLEMENT` และมีช่องกรอกยอดโอนจริงก่อนยิง `confirm_paid`; online order ปรับ action rail ให้เหลือ next step หลักจุดเดียวตามสถานะจริง (`ยืนยันชำระแล้ว/ตรวจสลิปและยืนยันชำระ` -> `แพ็กสินค้า` -> `จัดส่งแล้ว` -> `ยืนยันรับเงินปลายทาง (COD)`), เอาปุ่มซ้ำออกจาก `การทำงานเพิ่มเติม`, และปรับ stepper online ให้ตรง state machine จริง (`สร้างออเดอร์ -> ยืนยันชำระ -> แพ็กสินค้า -> จัดส่ง` หรือ `สร้างออเดอร์ -> แพ็กสินค้า -> จัดส่ง -> ปิดยอด COD` สำหรับ COD); pickup flow รองรับ 2 ลำดับ: `ยืนยันรับชำระ -> ยืนยันรับสินค้า` (ผ่าน `confirm_paid`) และ `ยืนยันรับสินค้า (ค้างจ่าย)` -> `ยืนยันรับชำระ` (ผ่าน action ใหม่ `mark_picked_up_unpaid` แล้วค่อย `confirm_paid` ปิดงาน); สำหรับออเดอร์หน้าร้าน `Walk-in/Pickup` ที่อยู่ในโหมด `ค้างจ่าย` (`channel=WALK_IN` + `paymentMethod=ON_CREDIT`) modal `ยืนยันรับชำระ` จะให้เลือก `เงินสด` หรือ `QR โอน` ก่อนบันทึก และถ้าเลือก QR ต้องเลือกบัญชี QR ของร้านเพื่อส่ง `paymentMethod/paymentAccountId` ไปกับ `confirm_paid`; เมื่อเลือกบัญชีแล้ว modal จะแสดง preview QR พร้อมเลขบัญชี และมี action ไอคอน `ดูรูปเต็ม`/`ดาวน์โหลด` รวมถึง `คัดลอกเลขบัญชี`; section `สถานะงาน` ใช้ responsive stepper (mobile = ขั้นปัจจุบัน+progress + compact stepper 1 แถวที่ label ตัดได้ 2 บรรทัด, desktop/tablet = stepper แนวนอนบรรทัดเดียว); สำหรับ `WALK_IN + PAID` ในหน้า detail ยังมีปุ่ม `ยกเลิกออเดอร์` (ตามสิทธิ์) ควบคู่กับปุ่มพิมพ์ใบเสร็จ; สำหรับ `WALK_IN + PENDING_PAYMENT`, `WALK_IN + CANCELLED`, ทุกเคส `READY_FOR_PICKUP` และ `PICKED_UP_PENDING_PAYMENT` ซ่อนเมนู `การทำงานเพิ่มเติม`; ปุ่ม `หน้าแพ็ก` เปิด `SlideUpSheet` บนหน้าเดิมแล้วและภายใน sheet ใช้ pack content แบบ packing slip พร้อมปุ่ม `พิมพ์ใบแพ็ก` ผ่าน `window.print()` โดยไม่ push route/ไม่เปิดแท็บใหม่; บล็อก `ชำระด้วย QR โอนเงิน` ในหน้า detail ใช้แนวทางงานจริงของลาวแล้ว: ตัด input `ลิงก์หลักฐานการชำระ` และปุ่ม `แนบหลักฐาน / ส่งรอตรวจสอบ` ออก, `Walk-in + ชำระแล้ว` เหลือเฉพาะ summary read-only (QR + ข้อมูลบัญชี) พร้อมปุ่ม `ดูรูปเต็ม` และ `ดาวน์โหลด QR` โดยปุ่มดาวน์โหลดใช้ route same-origin ก่อน, ส่วน `Pickup/Online` ให้ operator ตรวจสลิปจากแชต/ช่องทางภายนอกแล้วค่อยยืนยันผ่าน action หลักแทน และ backend `confirm_paid` ไม่บังคับ `paymentSlipUrl` สำหรับ flow นี้แล้ว; section `ข้อความสำหรับส่งลูกค้า` เหลือเป็น manual tools เท่านั้น และแสดงปุ่มตาม channel จริงของออเดอร์ (`คัดลอกข้อความ` เสมอ, `เปิด WhatsApp` เฉพาะออเดอร์ WhatsApp ที่มี deep link, `เปิด Facebook` เฉพาะออเดอร์ Facebook) พร้อมข้อความกำกับว่ายังไม่เชื่อม Facebook/WhatsApp API; บล็อกจัดส่งของ online เปลี่ยนเป็น manual upload-first โดยใช้ปุ่มเดียว `อัปโหลด/ถ่ายรูปป้าย` แล้วเปิด chooser แบบ `SlideUpSheet` ให้เลือก `เลือกรูปจากเครื่อง` หรือ `ถ่ายรูปจากกล้อง` (disable กล้องเมื่อเครื่องไม่รองรับ) บนมือถือสามารถ swipe down ปิดได้เหมือน modal อื่น ก่อนอัปโหลดและ PATCH `update_shipping` อัตโนมัติ; ถ้าไฟล์ป้ายไม่ใช่รูปหรือเกิน `6MB` จะ block ตั้งแต่ client พร้อมแสดงทั้ง toast และ inline error ใต้ปุ่มอัปโหลด, และถ้าเป็นไฟล์ raster ที่รองรับระบบจะย่อฝั่ง client เป็น `1600px WebP` ก่อนส่ง; ฝั่ง API ของ label upload รับเฉพาะ `JPG/PNG/WebP` และจะ optimize แบบ strict ก่อนเก็บ; ถ้ามีรูปป้ายอยู่แล้วสามารถกด `ลบรูปป้าย` เพื่อเคลียร์ `shippingLabelUrl` ออกจากออเดอร์ผ่าน custom confirm modal โดยไม่แตะข้อมูลขนส่งอื่น; current-page `พิมพ์ป้ายจัดส่ง` redesign เป็น shipping sticker 100x150mm แล้ว โดยใช้ `ผู้ฝาก`, `ผู้รับ`, `ที่อยู่ปลายทาง`, ติ๊ก `ค่าส่ง ต้นทาง/ปลายทาง`, block `COD`, ชื่อขนส่ง + dashed logo placeholder, ซ่อน tracking ถ้าไม่มีค่า, และย้าย QR ออเดอร์ไปไว้ล่างขวาโดยถอด text/hint ใต้ QR ออก; เพิ่มปุ่ม `ตีกลับเข้าร้าน (COD)` พร้อมช่องกรอก `ค่าตีกลับ` + `หมายเหตุสาเหตุ` ที่ยิง `mark_cod_returned` เพื่อบวกต้นทุนขนส่งและเก็บบันทึกสาเหตุตีกลับ โดยตรวจสิทธิ์ `orders.cod_return` เป็นหลัก; action `ยกเลิกออเดอร์` ใช้ modal กลาง 2 โหมด: `Owner/Manager` ใช้ `เหตุผล + สไลด์ยืนยัน` (`approvalMode=SELF_SLIDE`), role อื่นใช้ `ยืนยันรหัสผ่าน Manager` (`approvalMode=MANAGER_PASSWORD`), พร้อม cooldown เมื่อยืนยันไม่ผ่านติดกันหลายครั้ง; หลังยกเลิกแล้วหน้า detail จะแสดงสรุปการอนุมัติจาก `audit_events`) |
| `/orders/[orderId]/pack` | `app/(app)/orders/[orderId]/pack/page.tsx` | ไม่มี UI pack page แล้ว; route นี้ถูกคงไว้เป็น legacy redirect กลับ `/orders/[orderId]` เพื่อกันลิงก์เก่า/บุ๊กมาร์กพัง ขณะที่ flow pack จริงอยู่ใน `SlideUpSheet` ของหน้า detail |
| `/orders/[orderId]/print/receipt` | `app/(app)/orders/[orderId]/print/receipt/page.tsx`, `components/app/receipt-print-actions.tsx` | ไม่มี browser call ไป `/api` โดยตรงใน flow หลัก; หน้าใช้ server query `getOrderDetail` แล้วพิมพ์ receipt 80mm พร้อม summary/payment method และถ้าออเดอร์มี `paymentAccountQrImageUrl` จะพิมพ์ footer `QR รับชำระ` พร้อม `ชื่อบัญชี/ธนาคาร/เลขบัญชี` ต่อท้ายบิล โดยถ้ามี `paymentAccountId` จะใช้ `GET /api/orders/payment-accounts/[accountId]/qr-image` เป็นแหล่งรูป และถ้าไม่มี id จะ fallback ใช้ URL ของรูปโดยตรง |
| `/orders/[orderId]/print/label` | `app/(app)/orders/[orderId]/print/label/page.tsx` | ไม่มี browser call ไป `/api`; หน้าใช้ server query `getOrderDetail` แล้วพิมพ์ `shipping sticker` ขนาด 100x150mm โดยแสดง `ชื่อร้าน/ผู้ฝาก/เบอร์ร้าน`, `ผู้รับ`, `ที่อยู่ปลายทาง`, ติ๊ก `ค่าส่ง ต้นทาง/ปลายทาง`, `COD` เฉพาะออเดอร์ COD, ชื่อขนส่ง + dashed logo placeholder, ซ่อน tracking ถ้าไม่มีค่า, และย้าย `QR ออเดอร์` แบบ stable (`ORDER:${orderNo}`) ไปไว้ล่างขวาโดยไม่แสดง text/hint ใต้ QR |

## Products

| Page | Main Component | API Calls |
|---|---|---|
| `/products` | `components/app/products-management.tsx` | `GET/POST /api/products` (หน้า list ใช้ server-side pagination/filter/sort ผ่าน query `q`,`categoryId`,`status`,`sort`,`page`,`pageSize`; server render ของหน้า `/products` จะอ่าน `q/categoryId/status/sort` มาตั้งต้นให้ initial list ตรงกับ URL ก่อน hydrate และ client จะไม่ยิง refetch ซ้ำทันทีถ้า filter ปัจจุบันยังตรงกับ SSR seed เดิม; response มีค่าสต็อก `stockOnHand/stockReserved/stockAvailable` และ `costTracking` สำหรับแสดงที่มาของต้นทุนใน Product Detail, modal เพิ่ม/แก้ไขรองรับโหมด variant และมี Matrix Generator สำหรับสร้างหลายรุ่นย่อยแบบ bulk; ส่วนหน่วยแปลงรองรับ `ราคาขายต่อหน่วยแปลง` แบบ optional และมี sales-unit controls เพิ่ม: `products.allowBaseUnitSale` คุมว่าหน่วยหลักจะขายใน POS หรือไม่, `product_units.enabledForSale` คุมว่าหน่วยแปลงไหนเปิดขายใน POS; ตอนสลับแท็บสถานะมี client cache + abort stale request และแสดง skeleton loading ถ้ายังไม่มี cache; แท็บสถานะ sync กับ URL query `status` เพื่อคงแท็บเดิมหลัง hard refresh), `GET /api/products/models` (auto-suggest ช่อง `ชื่อสินค้าแม่ (Model)`, auto ตั้ง `ลำดับแสดง` จาก `nextSortOrder`, และ suggest `ชื่อ Variant` จาก `variantLabels`), `PATCH /api/products/[productId]` (action `update_cost` ต้องแนบ `reason`; multipart image upload จะย่อรูปฝั่ง client เป็น `640px WebP` ก่อนส่ง และฝั่ง API รับเฉพาะ `JPG/PNG/WebP` พร้อม optimize แบบ strict ก่อนเก็บ), `POST /api/products/generate-barcode` (ใช้ทั้งในฟอร์มปกติและเติม barcode ในตาราง matrix) (มีปุ่ม `รีเฟรช` แบบ manual ที่ header) |
| `/settings/store/payments` | `components/app/store-payment-accounts-settings.tsx` | `GET/POST/PATCH/DELETE /api/settings/store/payment-accounts` (หน้า settings บัญชีรับเงินของร้าน; state ปกติถอด page title ออกให้ card header เป็นหัวหลักของหน้า, card header มีปุ่ม `i` เปิด help sheet อธิบายกติกาหลักแทน helper text ยาว, ถอดข้อความ policy `บังคับแนบสลิปเมื่อจ่ายแบบ QR` ออกจากหน้าหลักเพื่อให้โฟกัสกับการตั้งค่าบัญชีโดยตรง, ใช้บัญชีเดียวที่มีข้อมูลธนาคารเสมอ + toggle `มี QR` แทนการแยก type ใน UX, เลือก `currency` เดียวต่อบัญชีได้เฉพาะในชุดที่ร้านเปิดไว้, และถ้าเปิด QR file picker จะรับเฉพาะ `JPG/PNG/WebP` โดยฝั่ง client validate แบบ raster-only และฝั่ง API จะ optimize แบบ strict ก่อนเก็บใน R2) |

## Stock & Purchase

- หน้า `/stock` ไม่มีปุ่มรีเฟรชรวมที่ header และใช้ `รีเฟรชแท็บนี้` เป็น action หลักต่อแท็บ
- `StockTabs` ใช้แบบ keep-mounted: เมื่อเข้าแท็บแล้วจะคง state เดิมไว้ตอนสลับไปมา
- server render ของหน้า `/stock` preload เฉพาะ active tab ตาม query `tab` แล้ว; แท็บอื่นใช้ skeleton ระหว่างรอ navigation fetch แทนการ preload ข้ามแท็บ
- แท็บ `inventory/history/recording/purchase` มีปุ่ม `รีเฟรชแท็บนี้` และแสดงเวลา `อัปเดตล่าสุด`

| Page | Main Component | API Calls |
|---|---|---|
| `/stock?tab=inventory` | `components/app/stock-inventory-view.tsx` | `GET /api/stock/products?page&pageSize&categoryId&q` (รีเฟรช/โหลดเพิ่มรายการสินค้าในแท็บดูสต็อก, กรองหมวดหมู่แบบ server-side, และค้นหา `sku/name/barcode` แบบ server-backed; server render ของหน้า `/stock` จะอ่าน `inventoryQ/inventoryCategoryId` มาตั้งต้นให้ initial list ตรงกับ URL), `GET /api/products/search?q&includeStock=true` (resolve ผลสแกนบาร์โค้ดแบบ exact barcode เพื่อใช้ feedback/toast และ guard กรณีติด filter หมวดหมู่); sync state ลง URL เฉพาะตอนแท็บ active ผ่าน `inventoryQ/inventoryFilter/inventorySort/inventoryCategoryId` โดยใช้ `window.history.replaceState(...)` สำหรับ query ภายในแท็บเพื่อลด route rerender; เมื่อสแกนบาร์โค้ด ระบบจะเติมค่าใน search input ทันทีและแสดง loading ระหว่างรอ stock fetch ใหม่; scanner UI/logic ใช้คอมโพเนนต์กลางเดียวกับ `/products` |
| `/stock?tab=history` | `components/app/stock-movement-history.tsx` | `GET /api/stock/movements?view=history&page&pageSize&type&q&productId&dateFrom&dateTo` (history server-side pagination/filter; list render แบบ windowed virtualization; UI แยก `search + scan` เป็น sticky bar ใต้ toolbar เพื่อให้เรียกค้น/สแกนซ้ำได้ระหว่างเลื่อนดูรายการ และคง card filter ด้านล่างสำหรับ `ประเภท` + `ช่วงวันที่ custom datepicker` + apply/clear; `historyQ` เป็น live search แล้วโดย debounce ประมาณ `350ms` แล้ว auto apply/fetch เอง ส่วน type/date ยังใช้ปุ่ม apply; sync filter/page ลง URL ด้วย `historyType/historyQ/historyDateFrom/historyDateTo/historyPage` และใช้ `window.history.replaceState(...)` สำหรับ query ภายในแท็บเพื่อลด route rerender, มี in-memory cache ต่อ filter key เพื่อสลับมุมมองเดิมได้เร็วขึ้น; รอบล่าสุดถ้ามี SSR/cached result สำหรับ key ปัจจุบันอยู่แล้วจะไม่ background refetch ทันทีตอนเปิดแท็บ; เมื่อสแกน barcode ระบบจะเติมค่าใน search input ก่อน, apply query ทันที, และแสดง loading inline ในช่องค้นหาพร้อมลด opacity ของผลลัพธ์เดิมระหว่างรอ fetch) |
| `/stock?tab=recording` | `components/app/stock-recording-form.tsx` | `POST /api/stock/movements` (ส่ง `Idempotency-Key` จาก client; บันทึก qty/unit/movementType; ถ้าส่ง field cost/rate จะโดน reject 400), `GET /api/stock/movements` (รีเฟรชสินค้า/ความเคลื่อนไหวในแท็บ), `GET /api/stock/products?productId=...&includeUnitOptions=true&page=1&pageSize=1` (lazy-load `unitOptions` พร้อม stock snapshot ของสินค้าที่กำลังเลือกเท่านั้นหลังเปิดแท็บหรือเปลี่ยนสินค้า; exact-product mode ใช้ fast path แยก); server render ของแท็บนี้จะเริ่มจาก product list แบบ base-unit-only ก่อนเพื่อลด SSR latency แล้วค่อยเติมหน่วยเพิ่มแบบ on-demand; UI มี guardrail/CTA ไปแท็บ PO โดยส่วนรายละเอียดยาวเป็นแบบพับ/ขยาย (default ปิด), มี product picker (`ดูสินค้าทั้งหมด`) + sticky action `บันทึกสต็อก`, และ sync filter ลง URL (`recordingType`, `recordingProductId`); scanner UI/logic ใช้คอมโพเนนต์กลางเดียวกับ `/products` |
| `/stock?tab=purchase` | `components/app/purchase-order-list.tsx` | หน้าแบ่งเป็น 3 workspace (`PO Operations`, `Month-End Close`, `AP by Supplier`) + summary strip ด้านบนแบบ KPI summary-only (ไม่คลิก); shortcut ใช้ saved preset chip/Applied filter, และ workspace switcher ใช้สำหรับนำทางหลัก (`PO Operations` เปิดด้วย filter `OPEN` เป็น default และจะเขียน query `poStatus` เมื่อผู้ใช้เลือกสถานะอื่น); รอบล่าสุดหน้า create/edit draft รองรับ `เลือกหน่วยซื้อ` ต่อรายการแล้ว โดยดึง `unitOptions` จาก stock products เดิม, แสดง preview ว่าแปลงกลับเป็นกี่หน่วยสต็อก, และ detail/PDF ของ PO จะแสดงทั้ง `qtyOrdered + purchase unit` และ `qtyBaseOrdered`; เพิ่ม extra cost currency แบบ pragmatic แล้ว โดย `ค่าขนส่ง/ค่าอื่น` ใน create/edit/apply-extra-cost เลือกได้เฉพาะ `สกุลร้าน` หรือ `สกุลซื้อของ PO`, UI จะแสดงยอดต้นฉบับคู่กับยอดฐานร้าน (`≈`) และ `finalize-rate` จะ recalc extra cost ที่ผูกกับ `purchaseCurrency` ให้ใหม่พร้อมต้นทุนสินค้า; `GET /api/stock/purchase-orders` (รีเฟรช/โหลดเพิ่มรายการ PO แต่จะไม่ auto-refetch ทันทีถ้ามี SSR initial list อยู่แล้ว), `GET/POST /api/stock/purchase-orders` (foreign currency สร้างแบบรอปิดเรทได้ + รองรับ `dueDate` + item payload ใช้ `unitId`), `GET /api/stock/purchase-orders/pending-rate` (คิวรอปิดเรท + filter ค้นหา `supplier/เลข PO/หมายเหตุ` ผ่าน `q` (alias `supplier`) + ช่วงวันที่รับของ; auto-load เฉพาะตอน workspace เป็น `Month-End Close`), `GET /api/stock/purchase-orders/[poId]` (detail ใช้ per-PO cache + intent-driven prefetch จาก hover/focus/touch และคง skeleton/retry เมื่อยังโหลดไม่เสร็จ), `POST /api/stock/purchase-orders/[poId]/finalize-rate` (ปิดเรทจริงหลังรับสินค้า; ใช้ได้ทั้งเดี่ยวและแบบกลุ่มจากคิวรอปิดเรท), `POST /api/stock/purchase-orders/[poId]/settle` (บันทึกชำระ PO แบบ partial/full; ใช้ได้ทั้ง workflow ปิดเดือนแบบกลุ่มในคิว pending-rate และ workflow เลือกหลาย PO จาก AP by Supplier โดยรองรับ manual-first statement allocation แบบ oldest due first), `POST /api/stock/purchase-orders/[poId]/apply-extra-cost` (อัปเดตค่าขนส่ง/ค่าอื่นภายหลังเมื่อ PO รับสินค้าแล้ว แต่ยังไม่ปิดจ่าย), `POST /api/stock/purchase-orders/[poId]/payments/[paymentId]/reverse` (ย้อนรายการชำระ), `GET /api/stock/purchase-orders/outstanding/export-csv` (export เจ้าหนี้ค้างจ่าย + FX delta), `GET /api/stock/purchase-orders/ap-by-supplier` + `GET /api/stock/purchase-orders/ap-by-supplier/statement` + `GET /api/stock/purchase-orders/ap-by-supplier/export-csv` (panel AP ราย supplier แบบ drill-down/filter/export; search supplier แยกจาก statement และ statement รองรับค้นหา `poNumber/note`; selection action bar ถูกย้ายไปติดกับ list PO และ sticky อยู่ในกรอบ list ระหว่างเลื่อน; เลือกหลายรายการเพื่อ bulk settle) |

## Reports

| Page | Main Component | API Calls |
|---|---|---|
| `/reports` | `app/(app)/reports/page.tsx`, `components/app/reports-filters.tsx` | ไม่มี browser call ไป `/api/reports`; หน้าใช้ server query `getReportsViewData` โดยรับ query string `preset/dateFrom/dateTo/channel` สำหรับ filter sales overview + daily trend + top products + sales by channel, และมีลิงก์ export `GET /api/stock/purchase-orders/outstanding/export-csv`; ส่วน COD/purchase snapshot ยังเป็นข้อมูล current state จาก query layer ฝั่ง server |
| `/reports/cash-flow` | `app/(app)/reports/cash-flow/page.tsx`, `components/app/cash-flow-filters.tsx` | ไม่มี browser call ไป `/api/finance`; หน้าใช้ server query `getCashFlowViewData` โดยรับ query string `preset/dateFrom/dateTo/direction/entryType/account` เพื่อสรุป `cash_flow_entries` เป็น summary cards, daily trend, by-account totals และ recent ledger feed; มีทางเข้าโดยตรงจาก `/reports` และ `/settings` |

## Settings

| Page | Main Component | API Calls |
|---|---|---|
| `/settings/profile` | `components/app/account-profile-settings.tsx`, `components/app/account-password-settings.tsx`, `components/app/account-settings-help-button.tsx` | `GET/PATCH /api/settings/account` (รองรับ action `update_profile`, `change_password`; state ปกติของหน้าถอด page title และ section label `โปรไฟล์/ความปลอดภัย` ออกแล้ว, รวม CTA ของ profile/password เป็น 2 action rows ภายใน parent card เดียวที่มี header `title + subtitle + ปุ่ม i`, และหัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว) |
| `/settings/language` | `components/app/account-language-settings.tsx`, `components/app/account-language-help-button.tsx` | `PATCH /api/settings/account` (action `update_locale`; state ปกติของหน้าถอด page title และ section label `display` ออกแล้ว, ใช้ parent card ที่มี header `title + subtitle + ปุ่ม i` และวาง CTA row ของภาษาไว้ด้านใน, และหัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว) |
| `/settings/permissions` | `app/(app)/settings/permissions/page.tsx`, `components/app/account-permissions-help-button.tsx` | page query ผ่าน session + RBAC helpers (`getUserPermissionsForCurrentSession`, `buildUserCapabilities`) โดยตรง; state ปกติของหน้าถอด page title ออกแล้ว, รวม summary กับรายการสิทธิ์ที่ใช้งานได้ไว้ใน parent card เดียวที่มี header `title + subtitle + ปุ่ม i`, และหัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว |
| `/settings/security` | `app/(app)/settings/security/page.tsx`, `components/app/account-security-help-button.tsx`, `components/app/account-password-settings.tsx`, `components/app/account-profile-settings.tsx` | page query ผ่าน `users` + `getGlobalSessionPolicy()` โดยตรง และ reuse `PATCH /api/settings/account` สำหรับ profile/password; state ปกติของหน้าถอด page title ออกแล้ว, รวมข้อมูลบัญชี + CTA เปลี่ยนรหัสผ่าน/โปรไฟล์ + tips ไว้ใน parent card เดียวที่มี header `title + subtitle + ปุ่ม i`, และหัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว |
| `/settings/roles` | `app/(app)/settings/roles/page.tsx`, `components/app/roles-list-help-button.tsx` | page query ผ่าน Drizzle โดยตรง (`roles`, `store_members`) และ state ปกติของหน้าถอด page title กับ section label `รายการบทบาท` ออกแล้ว โดยใช้ card header `title + subtitle` สำหรับรายการบทบาทแทน พร้อมปุ่ม `i` ที่เปิด help sheet |
| `/settings/users` | `components/app/users-management.tsx` | `GET/POST /api/settings/users`, `GET/PATCH /api/settings/users/[userId]`, `GET /api/settings/users/candidates` (state ปกติของหน้าถอด page title และ section label `จัดการสมาชิก` ด้านบนออกแล้ว, หัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว, member UI รวมเป็น card เดียวที่ใช้ header แบบ `title + จำนวนสมาชิก` พร้อมปุ่ม `i`/`เพิ่มสมาชิก`, และมี compact shortcut card สำหรับ `SUPERADMIN` ไปหน้า `/settings/superadmin/users`) |
| `/settings/categories` | `components/app/categories-management.tsx` | `GET/POST/PATCH/DELETE /api/products/categories` (state ปกติของหน้าถอด page title ออกแล้ว, card รายการหมวดหมู่ใช้ header `title + subtitle` พร้อมปุ่ม `i`/`เพิ่มหมวดหมู่`, และหัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว) |
| `/settings/stock` | `components/app/store-inventory-settings.tsx` | `GET/PATCH /api/settings/store` (state ปกติของหน้าถอด page title ออกแล้ว, card ตั้งค่า stock alerts ใช้ header `title + subtitle` พร้อม badge สถานะและปุ่ม `i` เปิด help sheet, และมี section `navigate` กลับหน้า settings) |
| `/settings/units` | `components/app/units-management.tsx` | `GET/POST /api/units`, `PATCH/DELETE /api/units/[unitId]` (state ปกติของหน้าถอด page title ออกแล้ว, card รายการหน่วยใช้ header `title + subtitle` พร้อมปุ่ม `i`/`เพิ่มหน่วยสินค้า`, และหัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว) |
| `/settings/store` | `components/app/store-profile-settings.tsx` | `GET/PATCH /api/settings/store` (หน้า `ข้อมูลร้าน` เหลือเฉพาะข้อมูลพื้นฐานของร้านแล้ว เช่น ชื่อร้าน โลโก้ ที่อยู่ และเบอร์ติดต่อ; การเงินร้านถูกแยกไปอีก route เพื่อให้หา config ง่ายขึ้น) |
| `/settings/store/finance` | `components/app/store-financial-settings.tsx` | `GET/PATCH /api/settings/store` (หน้า `การเงินร้าน` แยกจากข้อมูลร้านแล้ว; ใช้ตั้งค่า base currency, supported currencies และ VAT พร้อม route-level loading แยกของตัวเอง) |
| `/settings/store/payments` | `components/app/store-payment-accounts-settings.tsx` | `GET/POST/PATCH/DELETE /api/settings/store/payment-accounts` (บัญชีเดียว + toggle QR + currency เดียวต่อบัญชี) |
| `/settings/store/shipping-providers` | `components/app/store-shipping-providers-settings.tsx` | `GET/POST/PATCH/DELETE /api/settings/store/shipping-providers` (จัดการ master ผู้ให้บริการขนส่งของร้าน เพื่อใช้เป็นตัวเลือกใน flow `/orders/new` ออนไลน์; state ปกติถอด page title ออก, subtitle ยาวถูกถอดออก, card header มีปุ่ม `i` เปิด help sheet แทน helper text บนหน้า, และหัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว) |
| `/settings/pdf` | `components/app/store-pdf-settings.tsx` | `GET/PATCH /api/settings/store/pdf` (state ปกติของหน้าถอด page title ออกแล้ว, ใช้ card header `เมนูตั้งค่าเอกสาร PDF` เป็นหัวหลักของหน้า, และ card header มีปุ่ม `i` เปิด help sheet อธิบาย flow/preview ของการตั้งค่า PDF) |
| `/settings/notifications` | `components/app/notifications-inbox-panel.tsx`, `components/app/account-notifications-help-button.tsx` | `GET/PATCH /api/settings/notifications/inbox` (list + mark read/unread/resolve), `PATCH /api/settings/notifications/rules` (mute/snooze/clear ราย PO); state ปกติของหน้าถอด page title ออกแล้ว, ใช้ parent card ที่มี header `title + subtitle + ปุ่ม i` แล้ววาง cron hint กับ inbox panel ไว้ด้านใน, และหัวข้อ `navigate` ไม่ใช้ `tracking-[0.14em]` แล้ว |
| `/settings/stores` | `components/app/stores-management.tsx` | `POST /api/stores/switch`, `POST /api/stores/branches/switch`, `POST /api/onboarding/store`, `GET/POST /api/stores/branches` |
| `/settings/superadmin/global-config` | `components/app/superadmin-payment-policy-config.tsx`, `components/app/superadmin-global-config-help-button.tsx` | `GET/PATCH /api/settings/superadmin/payment-policy` (safe cleanup ล่าสุดเหลือ config จริงเฉพาะ `จำนวนบัญชีรับเงินสูงสุดต่อร้าน`; ไม่แสดง/ไม่แก้ `บังคับแนบสลิป QR` แล้ว) |
| `/settings/audit-log` | `app/(app)/settings/audit-log/page.tsx` | server query ตรง (no direct browser call to `/api`) |

## System Admin

| Page | Main Component | API Calls |
|---|---|---|
| `/system-admin` | `app/(system-admin)/system-admin/page.tsx` | ไม่มี browser call ตรงไป `/api`; ใช้ server query `getSystemAdminDashboardStats` และ `listSuperadmins` เพื่อแสดงภาพรวม + security signals และมีปุ่ม refresh (`router.refresh`) |
| `/system-admin/config` | `app/(system-admin)/system-admin/config/page.tsx` | ไม่มี browser call ตรงไป `/api`; เป็นเมนูนำทางไป config ต่าง ๆ |
| `/system-admin/config/clients` | `components/system-admin/superadmin-management.tsx` | `GET/POST /api/system-admin/superadmins`, `PATCH /api/system-admin/superadmins/[userId]` |
| `/system-admin/config/system` | `components/system-admin/system-branch-policy-config.tsx`, `components/system-admin/system-session-policy-config.tsx`, `components/system-admin/system-store-logo-policy-config.tsx` | `GET/PATCH /api/system-admin/config/branch-policy`, `GET/PATCH /api/system-admin/config/session-policy`, `GET/PATCH /api/system-admin/config/store-logo-policy` |
| `/system-admin/config/stores-users` | `components/system-admin/system-store-user-config.tsx` | `PATCH /api/system-admin/config/stores/[storeId]`, `PATCH /api/system-admin/config/users/[userId]` (โหลด list แรกเป็น server query ตรง) |
| `/system-admin/config/security` | `app/(system-admin)/system-admin/config/security/page.tsx`, `components/system-admin/system-security-snapshot.tsx` | ไม่มี browser call ตรงไป `/api`; query DB ตรงเพื่อสรุป JWT/Redis/session + audit signals |
| `/system-admin/config/monitoring` | `app/(system-admin)/system-admin/config/monitoring/page.tsx`, `components/system-admin/system-monitoring-snapshot.tsx` | ไม่มี browser call ตรงไป `/api`; query DB ตรงเพื่อสรุป Database/Cache/FB-WA health |

## Quick Debug Playbook

1. หา page จาก URL
2. เปิด component ตามตารางนี้
3. เช็ค API route ที่ map ไว้
4. ไล่ต่อไป service/repository ตาม `docs/CODEBASE_MAP.md`
